"""FinEvents — daily event/price correlation pipeline.

Module layout is fixed by Design 1. The two import rules on that layout are
enforced by tools/check_import_boundaries.py in CI, not by review (REQ-104,
REQ-1005).
"""

__version__ = "0.0.0"
